package test.webrtc.test.ui.utils

/*
@Composable
internal actual inline fun <reified VM : ViewModel> rememberViewModel(
    scope: ViewModelScope,
    key: String?,
    crossinline factory: @DisallowComposableCalls ViewModelsModule.() -> VM
) : VM = rememberViewModel(
    key = key,
    factory = factory
) {
    //ViewModelProvider.create()
    //LocalViewModelStoreOwner.current
    return viewModel<VM>(key = key, factory = viewModelFactory {
        initializer { it }
    })
}*/
